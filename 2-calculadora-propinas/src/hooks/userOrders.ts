import { useState } from "react";

export default function useOrder() {
  const [order, setOrder] = useState([]);
  console.log(order, setOrder([]));
  // Aun falta finalizar el contenido de este archivo
  // El curso esta pendiente por finalizar

  return {};
}
